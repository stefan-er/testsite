@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<aside class="col-md-3">
    <nav class="navbar" role="navigation">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle pull-left" data-toggle="collapse" data-target="#leftNav">
                <span class="icon-bar bg-primary"></span>
                <span class="icon-bar bg-primary"></span>
                <span class="icon-bar bg-primary"></span>
            </button>
        </div>
        <div id="leftNav" class="navbar-collapse collapse">
            @RenderSection("Aside", false)
        </div>
    </nav>
</aside>
<div class="col-md-9">
    @RenderBody()
</div>

@section scripts {
    @RenderSection("scripts", required: false)
}